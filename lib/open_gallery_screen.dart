import 'package:flutter/material.dart';

class OpenGalleryScreen extends StatelessWidget {
  const OpenGalleryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Open Gallery Screen"),
      ),
    );
  }
}